{
  "name": "@iboutsikas/coverpage",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite build --config vite.lib.config.ts",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/dist/bundle"]
    },
    "test": {
      "executor": "@nx/vite:test",
      "options": {
        "configFile": "{projectRoot}/vitest.config.ts"
      }
    }
  }
}
