{
  "name": "@iboutsikas/site",
  "implicitDependencies": ["@iboutsikas/coverpage", "@iboutsikas/router"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "dependsOn": [{ "projects": "@iboutsikas/coverpage", "target": "build" }, { "projects": "@iboutsikas/router", "target": "build" }],
      "options": {
        "parallel": true,
        "commands": [
          "cd packages/coverpage && vite build --watch --config vite.lib.config.ts",
          "cd packages/router && vite build --watch --config vite.lib.config.ts",
          "cd apps/hugo-site && hugo server --bind 0.0.0.0"
        ],
        "cwd": "{workspaceRoot}"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": [{ "projects": "@iboutsikas/coverpage", "target": "build" }, { "projects": "@iboutsikas/router", "target": "build" }],
      "options": {
        "command": "hugo",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/public"]
    }
  }
}
